{
	"name": "@omni-view/agent",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "cargo run",
		"build": "cargo build --release",
		"start": "cargo run --release",
		"clean": "cargo clean"
	}
}
