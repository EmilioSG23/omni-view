{
	"name": "@omni-view/agent",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "cargo run",
		"build": "cargo build --release",
		"start": "cargo run --release",
		"clean": "cargo clean",
		"test": "cargo test",
		"test:release": "cargo test --release",
		"test:doc": "cargo test --doc"
	}
}
