## Example environment for the web app
# Set the backend base URL (include /api if your backend exposes API under that path).
# In Vite, client-exposed env vars must be prefixed with VITE_
VITE_BACKEND_URL=http://localhost:4000/
