// Preload script — privileged bridge between Electron main and the web renderer.
// Future home for native capabilities: notifications, tray, system APIs, deep links.
// Use contextBridge.exposeInMainWorld() to safely expose APIs to the web app.

export {};
